<Page x:Class="AirFerry.Windows.Views.ReceiveTextView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      xmlns:ctrl="clr-namespace:AirFerry.Windows.Controls"
      Title="AirFerry - 文字消息"
      Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Navigation and completion status use separate rows. -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <Button Style="{StaticResource GhostButton}" Click="Back_Click"
                    HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE72B;" />
                    <TextBlock Text="返回" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Border Width="44" Height="44" CornerRadius="22"
                        Background="{DynamicResource SuccessBrush}">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE73E;"
                               FontSize="22" Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="文字接收完成"
                           FontSize="22" FontWeight="Bold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           VerticalAlignment="Center"
                           Margin="14,0,0,0" />
            </StackPanel>
        </StackPanel>

        <!-- Scrollable, read-only text content. -->
        <hc:Card Grid.Row="1">
            <TextBox x:Name="ContentBox"
                     IsReadOnly="True"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                     FontSize="15"
                     FontFamily="Consolas"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     Focusable="True"
                     Margin="12" />
        </hc:Card>

        <!-- Integrity status. -->
        <ctrl:InfoBanner Grid.Row="2"
                         x:Name="CrcInfoBar"
                         Severity="Informational"
                         Margin="0,12,0,0" />

        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <Button Style="{StaticResource ButtonPrimary}"
                    Click="Copy_Click"
                    FontSize="15" Height="40"
                    HorizontalAlignment="Stretch"
                    Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE8C8;" />
                    <TextBlock Text="复制到剪贴板" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        x:Name="SaveButton"
                        Click="SaveAs_Click"
                        HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE74E;" />
                        <TextBlock Text="保存为 .txt" Margin="7,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource GhostButton}"
                        Click="Rescan_Click"
                        HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE72C;" />
                        <TextBlock Text="重新扫码" Margin="7,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
