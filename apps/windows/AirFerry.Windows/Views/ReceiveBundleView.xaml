<Page x:Class="AirFerry.Windows.Views.ReceiveBundleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      xmlns:ctrl="clr-namespace:AirFerry.Windows.Controls"
      Title="AirFerry - 接收包"
      Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Navigation and completion status use separate rows. -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <Button Style="{StaticResource GhostButton}" Click="Back_Click"
                    HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE72B;" />
                    <TextBlock Text="返回" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Border Width="44" Height="44" CornerRadius="22"
                        Background="{DynamicResource SuccessBrush}">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE73E;"
                               FontSize="22" Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="接收完成"
                           FontSize="22" FontWeight="Bold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           VerticalAlignment="Center"
                           Margin="14,0,0,0" />
            </StackPanel>
        </StackPanel>

        <!-- Integrity status. -->
        <ctrl:InfoBanner Grid.Row="1"
                         x:Name="CrcInfoBar"
                         Severity="Informational"
                         Margin="0,0,0,12" />

        <hc:Card Grid.Row="2">
            <ListView x:Name="FileListView"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="文件名" Width="340"
                                        DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="大小" Width="140"
                                        DisplayMemberBinding="{Binding SizeText}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </hc:Card>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource ButtonPrimary}"
                    Click="SaveAll_Click"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE74E;" />
                    <TextBlock Text="全部保存" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Grid.Column="2" Click="OpenFolder_Click"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE838;" />
                    <TextBlock Text="打开文件夹" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Grid.Column="4" Style="{StaticResource GhostButton}"
                    Click="Rescan_Click"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource GlyphIcon}" Text="&#xE72C;" />
                    <TextBlock Text="重新扫码" Margin="7,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
