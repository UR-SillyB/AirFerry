# 2026-06-21

## 扫码端界面优化（Android ScanActivity.kt）

### 1. 删除火花动画（QrPositionOverlay）
- 移除了 `QrPositionOverlay` 整个 Composable 函数（含 Canvas 动画渲染）
- 移除了辅助函数：`refreshOverlay()`、`dedupeSparksBySlot()`、`slotScreenPos()`
- 移除了 companion 常量：`SPARK_LIFE_MS`、`SPARK_MAX`
- 保留 `SLOT_CENTER` 常量和 `gridSlotOf()` 函数（仍用于 per-code 状态追踪）
- 移除了 `overlayState`/`pendingSparks`/`sparkLock` 的所有引用
- 清理不再使用的 import：`LocalDensity`、`clip`、`LocalLifecycleOwner`

### 2. 替换为文字状态指示
- 在底部信息卡片新增"码状态"行，显示每个二维码的扫描状态：
  - 单码模式：`● 活跃` / `○ 暂停`
  - 多码模式：`①● ②○ ③· ④·`（●活跃/○暂停/·未扫描）
- 判断标准：1.5s（CODE_ACTIVE_MS）内收到新符号视为"活跃"
- 基于已有的 `codeActivity` 追踪系统（slot → last-timestamp）
- 添加 `codeActivitySnapshot` 到 `UiState`，用于 Compose 读取
- 添加 `codeStatusString()` 函数生成状态文本

### 3. 精简提示框信息
- 删除"已解码块"行（用户认为无意义）
- 合并"已扫描帧/已丢弃/丢帧率"为一行"帧统计"
- 合并"文件名/文件大小"为一行"文件"

## 构建与推送
- `./gradlew assembleRelease` 编译成功（11 个任务执行，41 个 UP-TO-DATE）
- 仅有 2 个 deprecation warning（startHighSpeed/stopHighSpeed，已下线）
- 通过 adb 无线推送 `app-release.apk`（47MB）到 `10.10.10.169:38243`
- 安装成功（`adb install -r`）

## 第二次迭代（码状态修复 + 帧统计格式调整）
- "丢N%" → "N%loss"，更简洁国际范
- 修复码状态识别：改用 `decodePool.snapshotMultiCount()` 判断单/多码模式，而非依赖 map 内容
- 添加 `multiCodeCount` 到 `UiState`，`codeStatusString` 据此显示正确数量的码
- 编译 → 推送 → 安装成功
